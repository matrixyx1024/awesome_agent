# 🏗️ Awesome Agent 架构设计

本文档详细说明 Awesome Agent 的架构设计，综合参考了 OpenClaw、LangChain、AutoGPT、CrewAI 等框架的最佳实践。

---

## 📐 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Client Layer                          │
│  CLI │ Web UI │ WebSocket Client │ HTTP Client          │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Gateway Layer                           │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ WebSocket    │  │ HTTP API     │                    │
│  │ Server       │  │ Server       │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────────────────────────┐                  │
│  │     Request Router               │                  │
│  │     Auth Handler                 │                  │
│  │     Event Broadcaster            │                  │
│  └──────────────────────────────────┘                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Agent Layer                             │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Agent Loop   │  │ Session      │                    │
│  │ Engine       │  │ Manager      │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Context      │  │ Queue        │                    │
│  │ Builder      │  │ Manager      │                    │
│  └──────────────┘  └──────────────┘                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Model Layer                             │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ OpenAI       │  │ Anthropic    │                    │
│  │ Provider     │  │ Provider     │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Qwen         │  │ Custom       │                    │
│  │ Provider     │  │ Provider     │                    │
│  └──────────────┘  └──────────────┘                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  Tool Layer                              │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Tool         │  │ Tool         │                    │
│  │ Registry     │  │ Executor     │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Browser      │  │ FileSystem   │                    │
│  │ Tool         │  │ Tool         │                    │
│  └──────────────┘  └──────────────┘                    │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │ Exec Tool    │  │ Custom       │                    │
│  │              │  │ Tools        │                    │
│  └──────────────┘  └──────────────┘                    │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Agent Loop 机制

### 执行流程（参考 OpenClaw）

```
1. 接收请求
   ├─ 验证参数
   ├─ 解析会话
   └─ 创建运行记录

2. Agent Loop
   ├─ 构建上下文
   │  ├─ 系统提示词
   │  ├─ 会话历史
   │  └─ 可用工具列表
   │
   ├─ 调用 LLM
   │  ├─ 生成回复
   │  └─ 解析工具调用
   │
   ├─ 执行工具（如果有）
   │  ├─ 验证权限
   │  ├─ 执行工具
   │  └─ 返回结果
   │
   └─ 继续循环或完成

3. 保存会话
   └─ 持久化到磁盘

4. 返回结果
   └─ 流式输出给客户端
```

### 循环控制

- **最大迭代次数**: 防止无限循环（默认 10 次）
- **超时控制**: 单次运行超时（默认 600 秒）
- **工具限制**: 每次循环最多调用 N 个工具

---

## 🛠️ 工具系统

### 工具注册

```typescript
// 工具必须实现 AgentTool 接口
interface AgentTool {
  name: string;
  description: string;
  parameters: Record<string, unknown>;
  execute(callId: string, args: Record<string, unknown>): Promise<unknown>;
}
```

### 工具执行流程

```
1. LLM 生成工具调用
   {
     "name": "browser",
     "arguments": { "action": "open", "url": "..." }
   }

2. Tool Registry 查找工具
   ├─ 检查工具是否存在
   ├─ 检查工具是否启用
   └─ 验证权限

3. 执行工具
   ├─ 验证参数
   ├─ 执行工具逻辑
   └─ 捕获错误

4. 返回结果
   └─ 清理敏感信息
```

### 权限控制

- **白名单模式**: 只允许列表中的工具
- **黑名单模式**: 禁止列表中的工具
- **会话级权限**: 不同会话可以有不同的工具权限

---

## 💾 会话管理

### 会话结构

```typescript
interface Session {
  id: string;           // 唯一会话 ID
  key: string;          // 会话键（用于路由）
  createdAt: number;     // 创建时间
  updatedAt: number;     // 更新时间
  messages: Message[];  // 消息历史
  metadata?: Record<string, unknown>; // 元数据
}
```

### 会话隔离策略

- **按用户隔离**: `user:<userId>`
- **按任务隔离**: `task:<taskId>`
- **按渠道隔离**: `channel:<channelId>:user:<userId>`

### 持久化

- **格式**: JSON 文件
- **位置**: `workspace/sessions/<sessionId>.json`
- **备份**: 支持会话导出和恢复

---

## 🌐 Gateway 架构

### WebSocket 协议

**消息格式:**
```typescript
// 请求
{
  type: 'req',
  id: string,
  method: string,
  params: Record<string, unknown>
}

// 响应
{
  type: 'res',
  id: string,
  ok: boolean,
  payload?: unknown,
  error?: { code: string; message: string }
}

// 事件
{
  type: 'event',
  event: string,
  payload: unknown
}
```

### 认证机制

- **Token 认证**: Bearer token
- **密码认证**: Basic auth
- **无认证**: 开发模式

---

## 🧠 模型集成

### 统一接口

所有模型提供商都实现相同的接口：

```typescript
interface ModelProvider {
  generate(options: GenerateOptions): Promise<GenerateResult>;
}
```

### 模型适配

- **OpenAI**: 直接使用 OpenAI SDK
- **Anthropic**: 使用 Anthropic SDK，适配消息格式
- **Qwen**: 使用 OpenAI 兼容 API

---

## 📊 事件系统

### 事件类型

1. **lifecycle**: 生命周期事件（start/end/error）
2. **assistant**: 助手输出（流式）
3. **tool**: 工具调用（start/update/end）
4. **error**: 错误事件

### 事件流

```
Agent Loop
  ├─ emit('lifecycle', { phase: 'start' })
  ├─ emit('assistant', { delta: '...' })
  ├─ emit('tool', { phase: 'start', name: 'browser' })
  ├─ emit('tool', { phase: 'end', name: 'browser' })
  └─ emit('lifecycle', { phase: 'end' })
```

---

## 🔒 安全设计

### 工具安全

- **参数验证**: 严格验证工具参数
- **路径检查**: 防止路径遍历攻击
- **命令过滤**: 阻止危险命令

### 会话安全

- **会话隔离**: 不同会话完全隔离
- **权限控制**: 基于会话的权限管理

### 网络安全

- **认证要求**: Gateway 默认需要认证
- **HTTPS 支持**: 支持 TLS（规划中）

---

## 🚀 性能优化

### 并发控制

- **会话级并发**: 每个会话串行执行
- **全局并发**: 限制同时运行的会话数
- **工具并发**: 支持并行工具执行（规划中）

### 上下文管理

- **历史修剪**: 自动修剪旧消息
- **压缩机制**: 压缩长上下文（规划中）
- **缓存**: 缓存常用数据（规划中）

---

## 📈 扩展性

### 插件系统（规划中）

- **工具插件**: 动态加载工具
- **模型插件**: 支持自定义模型提供商
- **Gateway 插件**: 扩展 Gateway 功能

### 多 Agent 协作（规划中）

- **Agent 团队**: 多个 Agent 协作
- **任务分配**: 自动分配任务
- **结果聚合**: 合并多个 Agent 的结果

---

## 🔄 与 OpenClaw 的对比

| 组件 | Awesome Agent | OpenClaw |
|------|---------------|----------|
| **Agent Loop** | ✅ 简化版 | ✅ 完整版 |
| **工具系统** | ✅ 基础工具 | ✅ 丰富工具 |
| **会话管理** | ✅ 基础功能 | ✅ 高级功能 |
| **Gateway** | ✅ WebSocket | ✅ WebSocket + HTTP |
| **多渠道** | ❌ | ✅ |
| **沙箱** | 🔄 规划中 | ✅ |
| **节点系统** | ❌ | ✅ |

---

## 💡 设计原则

1. **模块化**: 每个组件独立，易于替换
2. **可扩展**: 支持自定义工具、模型、插件
3. **类型安全**: 完整的 TypeScript 类型定义
4. **生产就绪**: 错误处理、日志、监控
5. **开发者友好**: 清晰的 API、完整的文档

---

## 🎯 未来规划

- **多 Agent 协作**: 实现 CrewAI 风格的多 Agent 系统
- **记忆系统**: 集成 LangChain 风格的记忆管理
- **任务规划**: 实现 AutoGPT 风格的自主规划
- **Web UI**: 构建可视化的控制面板
- **性能优化**: 提升并发能力和响应速度

---

**这就是 Awesome Agent 的架构设计！** 🚀
